version: "3.8"

services:

  contract_service:
    build: 
      context: .
      dockerfile: contract.Dockerfile
    container_name: contract_service
    ports:
      - "5004:5000"
    environment:
      PYTHONUNBUFFERED: 1
    env_file:
      - .env